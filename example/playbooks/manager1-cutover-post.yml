---
# tui: cutover
# Fixture: prints and exits. See example/README.md.
- name: Manager 1 cutover — post
  hosts: all
  gather_facts: false
  tasks:
    - name: Manager 1 cutover — post
      ansible.builtin.debug:
        msg: "{{ inventory_hostname }}: manager1-cutover-post would run here"
