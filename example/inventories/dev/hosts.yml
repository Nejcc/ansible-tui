---
# dev = local hypervisor VMs
all:
  children:
    dev:
      children:
        web:
          hosts:
            dev-web-1:
              ansible_host: 10.0.0.11
        db:
          hosts:
            dev-db-1:
              ansible_host: 10.0.0.12
  vars:
    env: dev
    provider: local
    ansible_connection: local
